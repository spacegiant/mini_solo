import 'package:flutter/material.dart';

class FactionView extends StatelessWidget {
  const FactionView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Faction');
  }
}
