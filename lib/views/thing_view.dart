import 'package:flutter/material.dart';

class ThingView extends StatelessWidget {
  const ThingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Thing');
  }
}
