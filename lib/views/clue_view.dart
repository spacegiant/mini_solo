import 'package:flutter/material.dart';

class ClueView extends StatelessWidget {
  const ClueView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Clue');
  }
}
