import 'package:flutter/cupertino.dart';

class ImportManager extends StatelessWidget {
  const ImportManager({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('IMPORT MANAGER');
  }
}
