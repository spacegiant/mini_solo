import 'package:flutter/material.dart';

class RandomButtons extends StatelessWidget {
  const RandomButtons({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Button Bar');
  }
}
