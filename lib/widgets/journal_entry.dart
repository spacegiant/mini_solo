import 'package:flutter/cupertino.dart';

class JournalEntry extends StatelessWidget {
  const JournalEntry({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(child: const Text('Welcome to the App!'));
  }
}
